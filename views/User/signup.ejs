<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>

    <!-- CSS -->
    <link rel="stylesheet" href="/css/registration.css">
            
    <!-- Boxicons CSS -->
    <link href='https://unpkg.com/boxicons@2.1.2/css/boxicons.min.css' rel='stylesheet'>
                    
</head> 
<body>
    <section class="container forms">
        <!-- Signup Form -->
        <div class="form signup">
            <div class="form-content">
                <% if (locals.msg) { %>
                    <header>Join FlickLounge<br> <p id="errorp"> <%=locals.msg%></p></header>
                <% } else { %>
                    <header>Join FlickLounge</header>
                <% } %>
                <form action="/user/signup" id="signup-form" method="POST">
                    <div class="field input-field">
                        <input type="username" name="username" placeholder="Username" class="input" id="signup-username" required pattern="^(?!^\d)(?!.*\.\.)(?!.*\.$)[\w.]{3,16}$" title="Username must start with a letter, can contain letters, numbers, underscores, hyphens, and periods, and must be between 3 and 16 characters long.">
                    </div>
                    <div class="field input-field">
                        <input type="email" name="email" placeholder="Email" class="input" id="signup-email" required>
                    </div>
                    <div class="field input-field">
                        <input type="password" name="password" placeholder="Create password" class="password" id="signup-password" required pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Must contain at least one number and one uppercase and lowercase letter, and at least 8 or more characters">
                    </div>
                    <div class="field input-field">
                        <input type="password" placeholder="Confirm password" class="password" id="confirm-password" required>
                        <i class='bx bx-hide eye-icon'></i>
                        <span id="password-match-error" class="error-message"></span>
                    </div>
                    <div class="field button-field">
                        <button type="submit">Signup</button>
                    </div>
                </form>
                <div class="form-link">
                    <span>Already have an account? <a href="/user/signin" class="loginbtn">Login</a>
                    </span>
                </div>
            </div>
            <div class="line"></div>
            <div class="media-options">
                <a href="#k" class="field facebook">
                    <i class='bx bxl-facebook facebook-icon'></i>
                    <span>Login with Facebook</span>
                </a>
            </div>
            <div class="media-options">
                <a href="#" class="field google">
                    <img src="/img/LoginForms/google.png" alt="" class="google-img">
                    <span>Login with Google</span>
                </a>
            </div>
        </div>
    </section>

    <!-- JavaScript -->
    <script src="/js/registration.js"></script>
</body>
</html>